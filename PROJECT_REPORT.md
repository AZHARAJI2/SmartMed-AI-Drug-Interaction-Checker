<div dir="rtl" align="right">

# 📋 التقرير الشامل — مشروع فحص تفاعلات الأدوية بالذكاء الاصطناعي

**Drug Package Image Interaction Checker — Full Project Report**

> **تاريخ التقرير**: سبتمبر 2026
>
> يُوثّق هذا التقرير كل تفاصيل المشروع من الفكرة إلى التنفيذ، شاملاً الهيكلة، التقنيات،
> المشاكل التي واجهتها وحلولها، ومراحل التطوير بالتفصيل.

---

## الفهرس

1. [مقدمة المشروع](#-مقدمة-المشروع)
2. [المشكلة التي يحلها المشروع](#-المشكلة-التي-يحلها-المشروع)
3. [الهندسة المعمارية](#-الهندسة-المعمارية-architecture)
4. [التقنيات والأدوات](#-التقنيات-والأدوات)
5. [مراحل التطوير](#-مراحل-التطوير)
6. [المرحلة الأولى — الرؤية الحاسوبية والتدريب](#المرحلة-الأولى--الرؤية-الحاسوبية-والتدريب)
7. [المرحلة الثانية — الخلفية ومحرك التفاعلات](#المرحلة-الثانية--الخلفية-ومحرك-التفاعلات)
8. [المرحلة الثالثة — واجهات المستخدم](#المرحلة-الثالثة--واجهات-المستخدم)
9. [قواعد البيانات](#-قواعد-البيانات)
10. [المشاكل والحلول](#-المشاكل-والحلول)
11. [الاختبارات](#-الاختبارات)
12. [التشغيل](#-التشغيل)

---

## 🎯 مقدمة المشروع

يهدف هذا المشروع إلى بناء **نظام ذكي متكامل** يستطيع:
- التعرف على الأدوية من صور عبواتها باستخدام تقنيات الذكاء الاصطناعي
- فحص التفاعلات الدوائية الخطيرة بين الأدوية تلقائياً
- تقديم تقارير طبية واضحة باللغة العربية للمريض والصيدلاني

النظام يُعالج مشكلة حقيقية تواجه المرضى الذين يتناولون أدوية متعددة دون علم بالتفاعلات الخطيرة بينها.

---

## 🔍 المشكلة التي يحلها المشروع

### المشكلة
- كثير من المرضى يتناولون عدة أدوية في نفس الوقت دون معرفة وجود تفاعلات خطيرة بينها
- الصيادلة يحتاجون أداة سريعة للتحقق من التعارضات الدوائية
- عدم وجود نظام آلي يتعرف على الأدوية من صور العبوات ويفحص التعارضات تلقائياً

### الحل
نظام يقوم بـ:
1. **تصوير عبوة الدواء** ← التعرف التلقائي على اسم الدواء (مساران: OCR + تصنيف صور)
2. **مقارنة الأدوية** بقاعدة بيانات DDInter (160,000+ تفاعل مسجل)
3. **إصدار تقرير** بدرجة الخطورة: آمن / تحذير / خطر
4. **توليد تقرير PDF** باللغة العربية لتقديمه للطبيب

---

## 🏗️ الهندسة المعمارية (Architecture)

### الفلسفة التصميمية
- **Domain-Driven Design**: كل مكوّن في مجلد منفصل بمسؤولية واحدة
- **Abstract Base Classes (ABC)**: واجهات مجردة تتيح تبديل الاستراتيجيات بسهولة
- **Strategy Pattern**: مثلاً FuzzyMatcher يدعم RapidFuzz أو difflib تلقائياً
- **Repository Pattern**: كل الوصول لقاعدة البيانات يمر عبر Repository classes
- **Fallback Design**: كل مكتبة خارجية لها بديل من المكتبة القياسية

### هيكل المجلدات

```
DL_IM_Project/
│
├── vision/                      # الرؤية الحاسوبية
│   ├── quality.py               # ImageQualityChecker — بوابة الجودة
│   ├── enhance.py               # ImageEnhancer(ABC) → CLAHEEnhancer, AdaptiveSharpener
│   └── explainer.py             # VisualExplainer — الشرح البصري
│
├── ocr/                         # قراءة النص
│   └── reader.py                # OCRReader(ABC) → EasyOCRReader (تحميل كسول)
│
├── classification/              # تصنيف الصور
│   ├── dataset.py               # DrugImageDataset — تحميل وتجهيز البيانات
│   ├── model.py                 # TransferLearningClassifier (MobileNetV3 + رأس جديد)
│   └── predictor.py             # Predictor — التنبؤ من صورة جديدة
│
├── fusion/                      # دمج النتائج
│   └── strategy.py              # FusionStrategy(ABC) → AgreementFusion
│
├── training/                    # التدريب والتقييم
│   ├── organize_data.py         # تنظيف البيانات وتقسيمها (train/val)
│   ├── trainer.py               # Trainer — حلقة التدريب
│   ├── evaluator.py             # Evaluator — مقاييس الأداء
│   └── ablation.py              # AblationStudy — دراسة الاستئصال
│
├── nlp/                         # معالجة النصوص
│   ├── cleaner.py               # TextCleaner — تنظيف OCR
│   ├── matcher.py               # FuzzyMatcher(ABC) → RapidFuzz / Difflib
│   └── normalizer.py            # IngredientNormalizer, CompoundDrugSplitter
│
├── core/                        # محرك النظام الأساسي
│   ├── engine.py                # InteractionChecker — فحص التفاعلات (safe/caution/danger)
│   ├── ddinter_loader.py        # DDInterIngestor — استيعاب بيانات DDInter
│   └── scan_pipeline.py         # ScanPipeline — خط الأنابيب الكامل
│
├── db/                          # قاعدة البيانات
│   ├── database.py              # محرك SQLAlchemy + الجلسات
│   ├── models.py                # النماذج: Drug, Ingredient, Interaction, User, ...
│   └── repositories.py          # 7 مستودعات (Repository classes)
│
├── api/                         # واجهة برمجة التطبيقات
│   ├── app.py                   # إنشاء تطبيق FastAPI + Swagger
│   ├── deps.py                  # حقن الاعتماديات (Dependency Injection)
│   ├── schemas.py               # مخططات Pydantic
│   ├── patient.py               # نقاط نهاية المريض (تسجيل/دخول/أدوية/فحص)
│   ├── doctor_tool.py           # نقاط نهاية الصيدلاني (فحص/مراجعة/بحث)
│   └── scan.py                  # نقطة نهاية مسح الصورة المشتركة
│
├── auth/                        # المصادقة
│   ├── security.py              # PasswordHasher + TokenCodec (ABC + fallbacks)
│   └── service.py               # AuthService — تسجيل/تسجيل دخول/JWT
│
├── ui/                          # واجهة المستخدم
│   ├── app.py                   # نقطة دخول Streamlit + مبدّل الأدوار
│   ├── patient_page.py          # صفحة المريض الكاملة
│   ├── doctor_page.py           # صفحة الصيدلاني الكاملة
│   ├── widgets.py               # مكونات مشتركة (تقارير، حالات، جداول)
│   ├── api_client.py            # عميل HTTP للتواصل مع API
│   └── pdf_report.py            # مولّد التقرير الطبي PDF (عربي)
│
├── correction_learning/         # تعلّم التصحيحات
│   └── logger.py                # CorrectionLogger + matching_dictionary.json
│
├── scripts/                     # أدوات مساعدة
│   ├── ingest_ddinter.py        # استيعاب ملفات DDInter CSV → قاعدة البيانات
│   └── seed_aliases.py          # بذر الأسماء البديلة الشائعة للأدوية
│
├── tests/                       # الاختبارات
│   ├── test_phase1_vision.py    # اختبارات المرحلة الأولى
│   ├── test_phase2_backend.py   # اختبارات المرحلة الثانية
│   └── test_phase3_ui.py        # اختبارات المرحلة الثالثة
│
├── config.py                    # الإعدادات المركزية
├── main.py                      # نقطة الدخول (uvicorn)
├── requirements.txt             # المتطلبات
└── README.md                    # التوثيق
```

### مخطط تدفق البيانات (Data Flow)

```
                    ┌─────────────────────┐
                    │   صورة عبوة الدواء    │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │  فحص الجودة (Quality) │
                    │  دقة / وضوح / إضاءة  │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │ تحسين الصورة (Enhance)│
                    │   CLAHE + Sharpening  │
                    └──────────┬──────────┘
                       ┌───────┴───────┐
              ┌────────▼────┐   ┌──────▼───────┐
              │  مسار OCR   │   │ مسار التصنيف  │
              │  (EasyOCR)  │   │ (MobileNetV3) │
              └────────┬────┘   └──────┬───────┘
                       └───────┬───────┘
                    ┌──────────▼──────────┐
                    │    دمج النتائج       │
                    │   (Fusion Strategy)  │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │  مطابقة في قاعدة     │
                    │  البيانات (Matching)  │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │  فحص التفاعلات       │
                    │  (DDInter Engine)     │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │ النتيجة: آمن/تحذير/خطر│
                    └─────────────────────┘
```

---

## 🔧 التقنيات والأدوات

### التعلم العميق والرؤية الحاسوبية

| التقنية | الوصف | الملف |
|---|---|---|
| **PyTorch 2.11** | إطار التعلم العميق — Transfer Learning | `classification/model.py` |
| **MobileNetV3-Large** | العمود الفقري المجمّد (Frozen Backbone) + رأس تصنيف جديد | `classification/model.py` |
| **EasyOCR 1.7** | محرك قراءة النصوص من الصور (عربي + إنجليزي) | `ocr/reader.py` |
| **OpenCV 4.8** | معالجة الصور: CLAHE, Sharpening, Quality Gate | `vision/` |
| **Pillow 10.1** | تحميل وتحويل تنسيقات الصور | مع PyTorch |

### الخلفية وقاعدة البيانات

| التقنية | الوصف | الملف |
|---|---|---|
| **FastAPI 0.141** | واجهة REST API مع Swagger التلقائي | `api/app.py` |
| **SQLAlchemy 2.0** | ORM لإدارة قاعدة البيانات | `db/` |
| **SQLite** | قاعدة بيانات مدمجة (ملف `app.db`) | `db/database.py` |
| **Pydantic 2.10** | التحقق من صحة البيانات (Validation) | `api/schemas.py` |
| **JWT (HS256)** | المصادقة — رموز وصول للمرضى | `auth/` |
| **bcrypt / PBKDF2** | تشفير كلمات المرور (مع fallback تلقائي) | `auth/security.py` |

### الواجهة والتقارير

| التقنية | الوصف | الملف |
|---|---|---|
| **Streamlit 1.50** | واجهة مستخدم تفاعلية (مريض + صيدلاني) | `ui/` |
| **httpx 0.28** | عميل HTTP للتواصل بين UI و API | `ui/api_client.py` |
| **ReportLab 5.0** | توليد تقارير PDF طبية باللغة العربية | `ui/pdf_report.py` |
| **arabic-reshaper + python-bidi** | دعم النص العربي RTL في PDF | `ui/pdf_report.py` |

### الأدوات والاختبارات

| التقنية | الوصف |
|---|---|
| **pytest 9.0** | إطار الاختبارات الآلية |
| **scikit-learn 1.7** | مقاييس الأداء (Accuracy, F1, Confusion Matrix) |
| **pandas 2.3** | معالجة البيانات الجدولية |
| **RapidFuzz / difflib** | مطابقة نصية ضبابية (مع fallback تلقائي) |

---

## 📐 مراحل التطوير

---

### المرحلة الأولى — الرؤية الحاسوبية والتدريب

**الهدف**: بناء خط أنابيب التعرف على الأدوية من الصور

#### ما تم إنجازه:

1. **تنظيم البيانات** (`training/organize_data.py`):
   - تنظيف أسماء الأصناف (Label Cleaning) من أسماء الصور
   - تقسيم البيانات إلى مجموعتي تدريب وتحقق (Stratified Split)
   - توليد ملف manifest يحتوي على كل الصور مع أسمائها وتقسيماتها
   - استبعاد الأصناف ذات العينات القليلة جداً تلقائياً

2. **بوابة الجودة** (`vision/quality.py`):
   - فحص الدقة الأدنى (128 بكسل)
   - فحص الوضوح (Laplacian Variance > 45)
   - فحص الإضاءة (V-channel: 35-235)

3. **تحسين الصورة** (`vision/enhance.py`):
   - CLAHE (تحسين التباين المحلي المحدود)
   - Adaptive Sharpening (شحذ تكيفي)
   - نمط Strategy Pattern — قابل للتبديل

4. **مسار OCR** (`ocr/reader.py`):
   - EasyOCR مع تحميل كسول (Lazy Loading)
   - تنظيف النص المقروء + مطابقة ضبابية مع قاعدة الأسماء

5. **مسار التصنيف** (`classification/`):
   - نموذج MobileNetV3-Large مع رأس تصنيف مخصص
   - التدريب بتقنية Transfer Learning (تجميد الطبقات الأساسية)
   - Data Augmentation (تدوير، قلب، تغيير ألوان)

6. **دمج النتائج** (`fusion/strategy.py`):
   - AgreementFusion: إذا اتفق المساران ← ثقة عالية
   - إذا اختلفا ← "غير مؤكد" (uncertain)
   - إذا أحدهما فارغ ← يُعتمد الآخر

7. **الشرح البصري** (`vision/explainer.py`):
   - رسم مستطيلات حول مناطق OCR
   - إضافة تسميات بالنتائج على الصورة

8. **التقييم والاستئصال**:
   - Accuracy, Precision, Recall, F1-Score
   - Confusion Matrix
   - Ablation Study (دراسة تأثير كل مكوّن)

---

### المرحلة الثانية — الخلفية ومحرك التفاعلات

**الهدف**: بناء قاعدة بيانات وAPI وآلية فحص التفاعلات الدوائية

#### ما تم إنجازه:

1. **قاعدة البيانات** (`db/`):
   - **Ingredient**: المكونات الفعالة (مع أسماء بديلة JSON)
   - **Drug**: الأدوية التجارية (مرتبطة بمكونات فعالة)
   - **Interaction**: التفاعلات الدوائية (ingredient_a ↔ ingredient_b + severity)
   - **User**: المستخدمون (مرضى)
   - **PatientMedication**: أدوية المريض
   - **ScanLog**: سجل عمليات المسح
   - **CorrectionLog**: سجل تصحيحات الصيدلاني

2. **استيعاب DDInter** (`core/ddinter_loader.py`, `scripts/ingest_ddinter.py`):
   - قراءة 8 ملفات CSV من DDInter
   - استخراج المكونات الفعالة والتفاعلات
   - تخزينها في SQLite (160,000+ تفاعل)

3. **محرك التفاعلات** (`core/engine.py`):
   - `InteractionChecker`: يستقبل قائمة أسماء أدوية
   - يُحلل الأسماء → مكونات فعالة (عبر normalizer + aliases)
   - يبحث في DDInter عن تفاعلات بين كل زوج
   - يُصنف النتيجة: **safe** / **caution** / **danger**

4. **تطبيع الأسماء** (`nlp/normalizer.py`):
   - `IngredientNormalizer`: تحويل الأسماء التجارية → مكونات فعالة
   - `CompoundDrugSplitter`: تفكيك الأدوية المركبة (مثل "paracetamol + ibuprofen")

5. **المصادقة** (`auth/`):
   - `PasswordHasher`: bcrypt (إن وُجد) أو PBKDF2-SHA256 (fallback)
   - `TokenCodec`: python-jose (إن وُجد) أو JWT HS256 يدوي (fallback)
   - `AuthService`: تسجيل + تسجيل دخول + إنشاء JWT

6. **واجهة API** (`api/`):
   - `POST /patient/register` — تسجيل حساب جديد
   - `POST /patient/login` — تسجيل دخول
   - `POST /patient/medications` — إضافة دواء (يقبل أي اسم)
   - `GET /patient/medications` — قائمة الأدوية
   - `DELETE /patient/medications/{id}` — حذف دواء
   - `POST /doctor/check` — فحص تعارض فوري
   - `POST /doctor/scan` — مسح صورة (بدون مصادقة)
   - `POST /scan/image` — مسح صورة (مع مصادقة المريض)
   - `GET /doctor/drugs?q=...` — بحث في قاعدة الأدوية
   - `POST /doctor/review` — تصحيح الصيدلاني

7. **خط أنابيب المسح** (`core/scan_pipeline.py`):
   - جودة → تحسين → OCR + تصنيف → دمج → مطابقة → تفاعلات → تسجيل

8. **تعلّم التصحيحات** (`correction_learning/logger.py`):
   - عندما يصحح الصيدلاني نتيجة خاطئة ← يُضاف الربط الجديد لقاموس المطابقة
   - يتحسن النظام تدريجياً مع الاستخدام

---

### المرحلة الثالثة — واجهات المستخدم

**الهدف**: بناء واجهة تفاعلية للمريض والصيدلاني

#### ما تم إنجازه:

1. **صفحة المريض** (`ui/patient_page.py`):
   - تسجيل حساب وتسجيل دخول (JWT)
   - إضافة أدوية بالاسم المباشر (أي اسم مقبول)
   - بحث اختياري في قاعدة البيانات
   - حذف أي دواء بضغطة زر
   - تصوير عبوة الدواء بالكاميرا مباشرة
   - رفع صورة من الجهاز
   - فحص تعارض تلقائي عند وجود دوائين أو أكثر
   - تقرير طبي PDF باللغة العربية
   - سجل الفحوصات السابقة

2. **صفحة الصيدلاني** (`ui/doctor_page.py`):
   - سلة فحص — إضافة أدوية بعدة طرق (كتابة، بحث، لصق قائمة)
   - فحص فوري للتعارض
   - مسح صورة مع شرح بصري (Explainability Overlay)
   - مراجعة وتصحيح النتائج
   - عرض آخر الفحوصات

3. **التقرير الطبي PDF** (`ui/pdf_report.py`):
   - واجهة عربية كاملة (RTL) باستخدام arabic-reshaper + python-bidi
   - خط Arial Unicode MS
   - جداول ملونة بحالات الخطورة
   - إخلاء مسؤولية طبي

4. **عميل API** (`ui/api_client.py`):
   - `ApiClient` — عميل HTTP متزامن (httpx)
   - `ScanView` — نموذج بيانات لنتائج المسح
   - إدارة الرموز (JWT tokens)

---

## 🗄️ قواعد البيانات

### جدول المكونات الفعالة (ingredients)
| الحقل | النوع | الوصف |
|---|---|---|
| id | INT PK | المعرف |
| name | VARCHAR(255) | الاسم العلمي |
| aliases | TEXT (JSON) | أسماء بديلة |
| drug_class | VARCHAR(255) | التصنيف الدوائي |

### جدول الأدوية (drugs)
| الحقل | النوع | الوصف |
|---|---|---|
| id | INT PK | المعرف |
| trade_name | VARCHAR(255) | الاسم التجاري |
| active_ingredient_ids | TEXT (JSON) | معرفات المكونات الفعالة |
| strength | VARCHAR(255) | التركيز |
| form | VARCHAR(255) | الشكل الصيدلاني |

### جدول التفاعلات (interactions)
| الحقل | النوع | الوصف |
|---|---|---|
| id | INT PK | المعرف |
| ingredient_a_id | INT FK | المكون الأول |
| ingredient_b_id | INT FK | المكون الثاني |
| severity | VARCHAR(32) | الخطورة (Major/Moderate/Minor) |
| description | TEXT | الوصف السريري |
| source | VARCHAR(255) | المصدر (DDInter) |

### جدول المستخدمين (users)
| الحقل | النوع | الوصف |
|---|---|---|
| id | INT PK | المعرف |
| name | VARCHAR(255) | الاسم |
| email | VARCHAR(255) UNIQUE | البريد الإلكتروني |
| password_hash | VARCHAR(512) | كلمة المرور المشفرة |

---

## 🐛 المشاكل والحلول

يوثّق هذا القسم **كل المشاكل التقنية التي واجهت المشروع** منذ بدايته، والسبب الجذري، والحل الذي طُبّق.

---

### المشكلة 1: ملف برومبت المرحلة الأولى تالف / محتوى خاطئ

- **الوصف**: الملف `02_Phase1_Execution_Prompt_Data_Training_EN-1.md` (434 كيلوبايت) لم يحتوي على برومبت التنفيذ إطلاقاً؛ بل كان صفحة HTML محفوظة لرسالة خطأ من موقع claude.com (`App unavailable in region`) — أي أنه عند نسخ البرومبت حُفظت صفحة الخطأ بدلاً من النص. اختفى كل محتوى تعليمات المرحلة الأولى.
- **الحل**: أُجري فحص شامل للمجلد (اكتُشف `01_Planning_Prompt_Master_EN.md` وقاعدتا البيانات `ddinter_database/` و `yemen_drug_database/`)، وبعد موافقة المستخدم أُعيد بناء المرحلة الأولى بالكامل من **برومبت التخطيط الرئيسي** بدلاً من الملف التالف.

---

### المشكلة 2: تعذّر تثبيت مكتبة RapidFuzz (الشبكة محجوبة)

- **الوصف**: `pip install rapidfuzz` يفشل بخطأ `WinError 10013` (وصول الشبكة ممنوع) في بيئة التطوير.
- **الحل**: تصميم `nlp/matcher.py` كواجهة مجردة `FuzzyMatcher(ABC)` مع استراتيجيتين قابلتين للتبديل:
  - `RapidFuzzMatcher` (تُستخدم فور تثبيت المكتبة مستقبلاً)
  - `DifflibMatcher` (fallback خالص من مكتبة Python القياسية، نشط حالياً)
- **الدرس**: تصميم كل مكوّن ليعمل بدون أي مكتبة خارجية مع الاستفادة منها حين تتوفر.

---

### المشكلة 3: غياب مكتبتي passlib و python-jose

- **الوصف**: `passlib[bcrypt]` و `python-jose` غير مثبتتين والشبكة محجوبة، وكانتا مقررتين في خطة التوثيق لأغراض المصادقة.
- **الحل**: `auth/security.py` يوفّر واجهتين مجردتين مع fallback تلقائي كامل:
  - `PasswordHasher` → bcrypt عبر passlib إن وُجد، وإلا PBKDF2-HMAC-SHA256 من المكتبة القياسية
  - `TokenCodec` → python-jose إن وُجد، وإلا JWT-صيغة HS256 قياسية (نفس الصيغة على السلك)
- **النتيجة**: المصادقة تعمل دون أي مكتبة إضافية، وتنتقل تلقائياً للخيار الأفضل عند توفرها.

---

### المشكلة 4: تجمّد طرفية VS Code بالكامل

- **الوصف**: خلال محاولة تحميل أوزان ImageNet (وزن MobileNetV3) علّقت عملية تحميل الشبكة الطرفية، وأصبحت كل الأوامر (حتى `Get-Location`) لا تُنفَّذ لعدة جلسات متتالية.
- **الحل**:
  - الاعتماد على أدوات الملفات (قراءة/تحرير) للمراجعة والبناء طوال فترة التجمّد
  - التوثيق الكامل للحالة في `agent_history.md`
  - استرجعت الطرفية بعد إعادة تشغيل VS Code، ثم أُجريت جميع الاختبارات الحية
- **الدرس**: عند أي تحميل كبير في المستقبل يُفضَّل تشغيله في عملية خلفية منفصلة.

---

### المشكلة 5: نقص استيراد `lru_cache` في `api/deps.py`

- **الوصف**: مرجع لدالة `lru_cache` دون استيرادها ← خطأ `NameError` عند استيراد وحدة الاعتماديات (توقف التطبيق كاملاً).
- **الحل**: إضافة `from functools import lru_cache`، مع إعادة هيكلة `get_config(request)` ليفضّل الإعداد المرتبط بالتطبيق (مهم للاختبارات المعزولة).

---

### المشكلة 6: خطأ 404 عند إضافة Aspirin من صفحة المريض

- **الوصف**: عند كتابة "Aspirin" في صفحة المريض، يظهر خطأ `[404] unknown drug 'Aspirin'` لأن اسم Aspirin لم يكن مسجلاً كاسم تجاري في قاعدة البيانات (كان مسجلاً كـ ingredient فقط).
- **الحل**: إعادة هيكلة دالة `add_medication` في `api/patient.py`:
  1. البحث في جدول الأدوية (case-insensitive)
  2. البحث في جدول المكونات الفعالة والأسماء البديلة
  3. إذا لم يُعثر على الدواء نهائياً ← **يُنشأ سجل مؤقت بدون ربطه بمكون فعال** (بدلاً من رفضه بـ 404)
- **النتيجة**: أي اسم دواء يُقبل الآن، مع رسالة واضحة إذا لم يكن في قاعدة DDInter.

---

### المشكلة 7: خطأ StreamlitDuplicateElementKey في صفحة الطبيب

- **الوصف**: `streamlit.errors.StreamlitDuplicateElementKey` يظهر في `doctor_page.py` عند عرض قائمة الأدوية، لأن مفاتيح العناصر (keys) لم تكن فريدة.
- **الحل**: إعادة كتابة `doctor_page.py` باستخدام سلة مبنية على `st.session_state.doctor_basket` مع مفاتيح فريدة لكل عنصر (تتضمن الفهرس + اسم الدواء + ختم زمني).

---

### المشكلة 8: رسائل تحذير مخيفة عند دواء واحد غير موجود في القاعدة

- **الوصف**: عند إدخال دواء واحد غير موجود في DDInter، يظهر تحذير أحمر "تنبيه — يوجد تعارض يستدعي الحذر" مع رسالة "أدوية غير مطابقة" — وهذا مُضلل لأنه لا يوجد تعارض فعلي مع دواء واحد.
- **الحل**:
  - تعديل `core/engine.py`: لا يُرقّى إلى `caution` إلا عند مقارنة دوائين أو أكثر
  - تعديل `ui/widgets.py`: رسالة معلوماتية (زرقاء) بدلاً من تحذير أحمر: "الدواء غير موجود في قاعدة DDInter — يُنصح بمراجعة الصيدلاني"
  - فصل واضح بين: أدوية في القاعدة / أدوية غير موجودة

---

### المشكلة 9: خطأ `Attribute "app" not found` في ASGI

- **الوصف**: عند تشغيل `uvicorn api.app:app`، يظهر خطأ لأن المتغير `app` لم يكن مُعرّفاً على مستوى الوحدة.
- **الحل**: إضافة `app = create_app()` في نهاية ملف `api/app.py` ليكون المتغير متاحاً لـ uvicorn.

---

### المشكلة 10: نتائج OCR تحتوي على كلمات غير دوائية (Junk)

- **الوصف**: عند مسح صورة، يقرأ OCR كلمات مثل "paolo monti servizio fotografico" (اسم المصور) أو "wikimedia" ويعاملها كأسماء أدوية، مما يسبب نتائج خاطئة.
- **الحل**: إضافة قائمة `junk_keywords` في `config.py` وتطبيقها في:
  - `core/scan_pipeline.py`: فلترة قبل المطابقة
  - `ui/widgets.py`: فلترة قبل العرض
  - `ui/patient_page.py` و `ui/doctor_page.py`: فلترة عند الإضافة اليدوية

---

## 🧪 الاختبارات

### هيكل الاختبارات

```
tests/
├── test_phase1_vision.py    # اختبارات الرؤية الحاسوبية والتدريب
├── test_phase2_backend.py   # اختبارات قاعدة البيانات و API والمحرك
└── test_phase3_ui.py        # اختبارات واجهة المستخدم
```

### تشغيل الاختبارات

```bash
# جميع الاختبارات
python -m pytest tests -q

# اختبارات محددة
python -m pytest tests/test_phase1_vision.py -v
python -m pytest tests/test_phase2_backend.py -v
python -m pytest tests/test_phase3_ui.py -v
```

### خصائص الاختبارات
- تعمل **بدون اتصال بالإنترنت** (offline)
- تستخدم **قاعدة بيانات مؤقتة** (temp SQLite) للعزل
- تستخدم **صور اصطناعية** (synthetic images) بدلاً من صور حقيقية
- OCR مُعطّل في الاختبارات لتسريعها
- اختبارات UI تمر عبر **ASGI Transport** بدون فتح socket

---

## 🚀 التشغيل

### المتطلبات
- Python 3.12+
- Windows 10/11

### خطوات التشغيل الكاملة

```bash
# 1. تثبيت المتطلبات
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt

# 2. بناء قاعدة البيانات
python scripts/ingest_ddinter.py
python scripts/seed_aliases.py

# 3. تشغيل الخادم الخلفي
uvicorn main:app --reload
# Swagger: http://127.0.0.1:8000/docs

# 4. تشغيل واجهة المستخدم (في طرفية منفصلة)
streamlit run ui/app.py
# الواجهة: http://localhost:8501
```

### متغيرات البيئة الاختيارية
- `DL_IM_API_URL`: عنوان API مخصص (الافتراضي: `http://127.0.0.1:8000`)

---

## 📝 ملاحظات إضافية

- **التقرير الطبي PDF**: يُنتج باللغة العربية بالكامل مع دعم RTL
- **أي اسم دواء مقبول**: حتى لو لم يكن في القاعدة — يُحفظ ويُشار إليه بأنه غير مُفحوص
- **تعلّم ذاتي**: تصحيحات الصيدلاني تُحسّن المطابقة تدريجياً
- **Offline-first**: كل المكتبات الاختيارية لها بدائل من المكتبة القياسية

</div>
